<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="webapp.mapper.UserMapper">

    <!-- 定义 resultMap 映射 -->
    <resultMap id="userMap" type="User">
        <id property="id" column="id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insertUser" parameterType="User">
        insert into user
        (id, name, age, address, sex, tel_number, qq, account, password, create_time)
        values (#{id}, #{name}, #{age}, #{address}, #{sex}, #{tel_number}, #{qq}, #{account}, #{password}, #{createTime})
    </insert>

    <select id="getUserById" resultType="User">
        select * from user where id = #{id}
    </select>

    <delete id="deleteUser">
        delete from user where id = #{id}
    </delete>

    <update id="updateUser">
        update user
        set name= #{name},
            age=#{age},
            address=#{address},
            sex=#{sex},
            tel_number=#{tel_number},
            qq=#{qq},
            account=#{account},
            password=#{password}
        where id = #{id}
    </update>

    <select id="selectUserByPage" resultType="User" resultMap="userMap">
        select * from user where 1=1
        <if test="name != null and name.trim().length() > 0">
            and name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="phone != null and phone.trim().length() > 0">
            and tel_number LIKE CONCAT('%', #{phone}, '%')
        </if>
        <if test="sex != null and sex.trim().length() > 0">
            and sex = #{sex}
        </if>
        order by id_sort desc
    </select>

    <select id="getTotal" resultType="int">
        select COUNT(*) from user;
    </select>

    <select id="findUserByNameAndPwd" resultType="User">
        select * from user where name = #{name} and password = #{password}
    </select>
</mapper>